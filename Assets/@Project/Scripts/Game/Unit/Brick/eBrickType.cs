// Brick types — Brick.md §2.
// Currently a code enum for v0.1 simplicity. Promote to #enum sheet when SpecBrick table is added.
public enum eBrickType
{
    NORMAL    = 0,
    SHIELD    = 1,
    EXPLOSIVE = 2,
    SPAWNER   = 3,
    REFLECTOR = 4,
    BOSS      = 5,
}
